{
  "_comment": "minimum_coverage is a don't-regress ratchet set just below current (~63.5%), not a target. Raise it as coverage improves. Excludes test support files and the generator mix task.",
  "coverage_options": {
    "minimum_coverage": 60
  },
  "skip_files": [
    "test/",
    "lib/mix/tasks/mercato.install.ex"
  ]
}
